\begin{tikzpicture}
    % Barvy
    \colorlet{backgroundColor}{accentColor!85!black}
    \colorlet{axisColor}{accentColor!60!black}
    \colorlet{graphColor}{accentColor!30!black}

    % Zvětšení
    \def \scalex {2.5}
    \def \scaley {2.5}

    % Tloušťka čáry
    \def \linethin {1pt}
    \def \linethick {2pt}

    \draw[color=backgroundColor] (0, 5) node[anchor=center] {\fontsize{90pt}{100pt}\selectfont $f: \reals \mapsto \reals$};
    
    % Osa x
    \draw[color=axisColor, line width=\linethin] (-9, 0) -- (9, 0);
    \foreach \x in {-3,...,3} {
        \ifnum \x = 0 {} \else
            \MULTIPLY{\x}{\scalex}{\xs}
            \draw[line width=\linethin, color=axisColor] (\xs, 0.2) -- (\xs, -0.2)
            node[anchor=north, inner sep=2mm] {\Huge $\x$};
        \fi
    }

    % Osa y
    \draw[color=axisColor, line width=\linethin] (0, -10) -- (0, 17.5);
    \foreach \y in {-3,...,6} {
        \ifnum \y = 0 {} \else
            \MULTIPLY{\y}{\scaley}{\ys}
            \draw[line width=\linethin, color=axisColor] (0.2, \ys) -- (-0.2, \ys)
            node[anchor=east, inner sep=2mm] {\Huge $\y$};
        \fi
    }

    % Graf
    \directlua{
        local luaimp = require("chapters/funkce/chapterimage");
        luaimp.graph();
    }
\end{tikzpicture}